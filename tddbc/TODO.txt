【閉区間】
[x]下端点を取得する
[x]上端点を取得する
[x]コンストラクタを生成する
[/]デストラクタを生成する
[x]コンストラクタの引数は下端点と上端点の順で値を取る
[x]コンストラクタの引数が下端点＜上端点の場合はOKとなる
[x]コンストラクタの引数が下端点＞上端点の場合はエラーとなる
[x]コンストラクタの引数が下端点＝上端点の場合はOKとなる // 仕様がないものは確認しなければいけない。(確認済)
[/]文字列表記を取得する // 実装しない


下端点と上端点を与えて閉区間を生成しよう
閉区間から下端点と上端点を取得しよう
閉区間から文字列表記を取得しよう
両端は区間に含まれる

【閉区間が指定した整数を含むか判断しよう】
[x]区間内の値はtrueを返す
[x]区間外の値はfalseを返す
[x]区間下端の値はtrueを返す
[x]区間上端の値はtrueを返す

【リファクタリングTodo】
[ ]同じパラメタのテストをまとめる


【閉区間が別の閉区間と重なっているか判定しよう】
[x]閉区間Ａと閉区間Ｂが等しい場合はTrueを返す
[x]閉区間Ａと閉区間Ｂが等しくない場合はfalseを返す

【閉区間が別の閉区間と接続しているか判定しよう】
[x]閉区間Aと閉区間Bがまったく重なっていない場合はFalseを返す
[x]閉区間Aの上限と閉区間Bの下限が一致している場合はTrueを返す
[x]閉区間Aの下限と閉区間Bの上限が一致している場合はTrueを返す
[x]閉区間Aの範囲内に閉区間Bが入り込んでいる場合はTrueを返す
[o]閉区間Aが閉区間Bの範囲内に入り込んでいる場合はTrueを返す
